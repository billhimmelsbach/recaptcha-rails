<%= form_for(@switt) do |f| %>
  <% if @switt.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@switt.errors.count, "error") %> prohibited this switt from being saved:</h2>

      <ul>
      <% @switt.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :super_power %><br>
    <%= f.text_field :super_power %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
